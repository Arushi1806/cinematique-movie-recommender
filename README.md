Place your trained model files here:

- `movies.pkl`
- `similarity.pkl`

These are loaded by `recommender.py`. They are not included in this
refactor since they're your trained artifacts from PyCharm.
